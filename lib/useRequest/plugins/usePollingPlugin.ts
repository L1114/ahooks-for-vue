import { Plugin } from "../type";

import { onMounted, onUnmounted, watch, Ref, isRef, ref } from "vue-demi";
const usePollingPlugin: Plugin<any, any[]> = (fetchInstance, {}) => {
  return {};
};
export default usePollingPlugin;
